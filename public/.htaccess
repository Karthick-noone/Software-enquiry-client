RewriteEngine On

# Keep API requests and existing files/directories out of the SPA fallback.
RewriteCond %{REQUEST_URI} !^/api(?:/|$)
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.html [L]